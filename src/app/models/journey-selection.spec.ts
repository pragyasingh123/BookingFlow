import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import { JourneySelection } from './journey-selection';

describe('Location', () => {
  it('should create an instance', () => {
    // expect(new JourneySelection()).toBeTruthy();
  });
});
