import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {ExtraPutResponse} from './extra-put';

describe('ExtraPut', () => {
  it('should create an instance', () => {
    // expect(new Extra()).toBeTruthy();
  });
});
