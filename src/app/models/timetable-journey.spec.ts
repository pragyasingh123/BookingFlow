import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {TimetableJourney} from './timetable-journey';

describe('TimetableJourney', () => {
  it('should create an instance', () => {
    // expect(new TimetableJourney()).toBeTruthy();
  });
});
