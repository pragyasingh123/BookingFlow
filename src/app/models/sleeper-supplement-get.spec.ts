import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import { SleeperSupplementGetRequest } from './sleeper-supplement-get';

describe('ExtraGet', () => {
  it('should create an instance', () => {
    // expect(new Extra()).toBeTruthy();
  });
});
