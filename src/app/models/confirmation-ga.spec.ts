/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {ConfirmationGA} from './confirmation-ga';

describe('Basket', () => {
  it('should create an instance', () => {
    expect(new ConfirmationGA()).toBeTruthy();
  });
});
