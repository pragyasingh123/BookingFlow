/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {Journey} from './journey';

describe('Journey', () => {
  it('should create an instance', () => {
    expect(new Journey()).toBeTruthy();
  });
});
