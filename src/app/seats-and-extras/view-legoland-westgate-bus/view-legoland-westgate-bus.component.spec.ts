import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';

import { ViewLegolandWestgateBusComponent } from './view-legoland-westgate-bus.component';

describe('Component: ViewLegolandBus', () => {
  it('should create an instance', () => {
    // let component = new ViewLegolandWestgateBusComponent();
    // expect(component).toBeTruthy();
  });
});
