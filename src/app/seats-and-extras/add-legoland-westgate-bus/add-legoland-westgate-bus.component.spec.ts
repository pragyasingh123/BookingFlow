import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';

import { AddLegolandWestgateBusComponent } from './add-legoland-westgate-bus.component';

describe('Component: AddLegolandBus', () => {
  it('should create an instance', () => {
    // let component = new AddLegolandWestgateBusComponent();
    // expect(component).toBeTruthy();
  });
});
