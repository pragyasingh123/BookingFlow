import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';

import { ReturnJourneySelectorComponent } from './return-journey-selector.component';

describe('Component: ReturnJourneySelector', () => {
  it('should create an instance', () => {
    // let component = new ReturnJourneySelectorComponent();
    // expect(component).toBeTruthy();
  });
});
