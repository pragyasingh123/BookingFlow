import { TestBed, async } from '@angular/core/testing';
import { TicketFirstCardComponent } from './ticket-first-card.component';

describe('Component: TicketFirstCard', () => {
  it('should create an instance', () => {
    let component = new TicketFirstCardComponent();
    expect(component).toBeTruthy();
  });
});
