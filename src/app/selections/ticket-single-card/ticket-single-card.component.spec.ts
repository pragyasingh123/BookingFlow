import { TestBed, async } from '@angular/core/testing';
import { TicketSingleCardComponent } from './ticket-single-card.component';

describe('Component: TicketSingleCard', () => {
  it('should create an instance', () => {
    let component = new TicketSingleCardComponent();
    expect(component).toBeTruthy();
  });
});
