import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';
import {JourneyService} from '../services/journey.service';
import {BasketService} from '../services/basket.service';

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';

import { OutwardJourneySelectorComponent } from './outward-journey-selector.component';

describe('Component: OutwardJourneySelector', () => {
  it('should create an instance', () => {
    // todo OutwardJourneySelectorComponent needs arguments for constructor instantiatied within test spec - imports above
    // let component = new OutwardJourneySelectorComponent();
    // expect(component).toBeTruthy();
  });
});
