import { TestBed, async } from '@angular/core/testing';
import { SessionNotificationComponent } from './session-notification.component';

describe('Component: SessionNotification', () => {
  it('should create an instance', () => {
    let component = new SessionNotificationComponent();
    expect(component).toBeTruthy();
  });
});
