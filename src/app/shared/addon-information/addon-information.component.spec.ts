import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import { AddonInformationComponent } from './addon-information.component';

describe('Component: AddonInformation', () => {
  it('should create an instance', () => {
    // let component = new AddonInformationComponent();
    // expect(component).toBeTruthy();
  });
});
